What is helm?
this is a package manager for kubernetes , this is essentially a way to bound and bind a bunch of manifest files

So we bundle all the files together and then we have them in a repo and then we release it 

The files we have together is like deployment service , roles etc

the helm charts are present in artifacthub.io

helm create helm_name , this will create a sample folder with the yaml we need and might use