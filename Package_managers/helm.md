What is helm?
this is a package manager for kubernetes , this is essentially a way to bound and bind a bunch of manifest files

So we bundle all the files togther and then we have them in a repo and then we release it 

The files we have tother is like deployment service , roles etc

the helm charts are present in artifacthub.io

helm create helm_name , this will create a sample foler with the taml we need and might use